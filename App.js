// Not used at runtime — the app's real entry point is expo-router/entry
// (see package.json's "main" field, and app/_layout.tsx for the actual root
// layout, which is what Metro actually loads). This stub exists only
// because some tooling (notably Expo Snack's "Import Git repository"
// feature) expects a root App.js/App.tsx to exist and fails during import
// without one.
import 'expo-router/entry';
